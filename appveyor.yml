environment:
  matrix:
    - GENERATOR: "Visual Studio 12"
      CONFIG: Debug

    - GENERATOR: "Visual Studio 12"
      CONFIG: Release

    - GENERATOR: "Visual Studio 12 Win64"
      CONFIG: Debug

    - GENERATOR: "Visual Studio 12 Win64"
      CONFIG: Release

build_script:
  - make

test_script:
  - test/testrun.bat
